export type City = {
    city: string,
    country: string,
    count: number
}

export type CityData = {
    city: string,
    country: string,
    count: number,
    normalizedCountry: string,
    x: number,
    y: number,
}